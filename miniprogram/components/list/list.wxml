<view class="list-container">
  <view class="title"wx:if="{{open}}"  bindtap="open">
    点击收回
  </view>
  <view class="title"wx:if="{{!open}}"  bindtap="open">
    点击展开
  </view>
  <view class="item" wx:for="{{list}}" wx:if="{{open}}">
    {{item.n}}
  </view>
</view>